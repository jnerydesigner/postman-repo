$kind: http-request
name: erp/v1/perfis
url: https://api.spicbrasil.com.br/erp/v1/perfis?$skip=0&$top=50000
method: GET
headers:
  client_id: b384e532-40c3-435b-9670-f935d774535c
  Authorization: Bearer {{ACCESS_TOKEN}}
queryParams:
  $skip: "0"
  $top: "50000"
scripts:
  - type: afterResponse
    code: |-
      const jsonData = pm.response.json();
      const qtdPerfs = jsonData.perfis.length;

      const perfis = jsonData.perfis.find(usuario => usuario.idSap === "AANDRADE");

      console.log(qtdPerfs)
      console.log(perfis)
    language: text/javascript
settings: {}
order: 20000
