$kind: http-request
url: "{{urlBaseDirectus}}/items/tech_skill"
method: POST
body:
  type: json
  content: |-
    {
      "name": "DevOps",
      "slug": "devops"
    }
auth:
  type: bearer
  credentials:
    token: "{{token}}"
settings: {}
order: 15000
